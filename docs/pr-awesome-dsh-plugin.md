# PR: Add dsh-minimax-usage to awesome list

## Entry

```markdown
- [dsh-minimax-usage](https://github.com/Floating-Dreaming/dsh-minimax-usage) — Settings page "用量" section that shows your MiniMax Token Plan usage (5h / weekly windows + per-model breakdown) by calling `/v1/token_plan/remains` via a trusted plugin. Auto-hides when API key isn't configured. MIT.
```

## Suggested category

`Settings` or `Usage display` (whichever fits the list structure).

## Why

DSH users who have MiniMax Token Plan need a quick way to see remaining quota without leaving the agent. This plugin adds a one-glance Settings panel.

## Verified

- Tested on Windows 11 + macOS
- Cross-platform install scripts (PowerShell + bash)
- Publishable to npm + GitHub
- 17-version changelog documenting the design journey

## Repo

https://github.com/Floating-Dreaming/dsh-minimax-usage